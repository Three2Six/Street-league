export const STANDARD_AVATARS = ['🚗', '🚙', '🏎️', '🛻', '🏍️', '🚘', '🚕', '🚐'];

// Only selectable by users whose account has founder === true — locked in at signup time for
// anyone who joined while the beta was open.
export const FOUNDER_AVATARS = ['👻', '🐌'];

// Hover labels — some of these emoji read as generic shapes on their own, so name them explicitly.
export const AVATAR_LABELS = {
  '👻': 'Ghost',
  '🐌': 'Snail',
};
